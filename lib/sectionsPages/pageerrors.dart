import 'package:flutter/material.dart';

class PagErrors extends StatelessWidget{
  const PagErrors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Page Not exist"),
      ),
    );
  }

}